# Cline 项目规则 — Job Engine

---

## 1. 物理通信红线（禁止乱码）

### 【强制文件化】
严禁在终端（Terminal）中使用 `python -c` 注入超过 5 行的代码。任何复杂的逻辑调试、测试、抓取任务，必须先创建 `.py` 临时文件，通过 `conda run -n job_env python filename.py` 执行。

### 【零容忍截断】
若终端输出出现 `<ffffffff>`、`dquote>` 或引号不闭合导致的挂起，Cline 必须立即中止任务，清理缓冲区，并自动切换为"文件驱动模式"重试。

---

## 2. 抓取与同步范式（防御式编程）

### 【原始数据优先】
在详情页提取时，若 CSS 选择器失效或无法精准识别标题（如小红书），必须采用"全量降级抓取"：使用 `page.locator('body').inner_text()` 抓取全文，先确保存入 `full_jd`，严禁因找不到特定字段而返回空数据。

### 【原子化保存】
抓取脚本必须具备"每成功一条即刻写入 JSON"的机制。严禁在内存中堆积全部数据后再统一保存，以防中途崩溃导致数据全失。

### 【同步前置检查】
在运行 `openclaw_bridge.py` 之前，必须先检查本地 JSON 文件的大小和格式。若文件为空或大小异常（<1KB），严禁触发 Notion 写入流程。

---

## 3. 环境与路径规范

### 【环境锁定】
本项目所有执行指令必须强制前缀 `conda run -n job_env`。禁止在 base 环境或其他未授权环境下尝试安装库或运行脚本。

### 【路径绝对化】
在脚本内部引用 `data/` 目录或配置文件时，必须使用相对于项目根目录的绝对路径或 `pathlib`，防止因执行路径不同导致的 `FileNotFound` 错误。

---

## 4. 数据清理与幂等性

### 【查重逻辑强制化】
同步脚本必须以 URL 为唯一主键。在执行写入前，必须先调用 Notion API 检索该 URL 是否已存在。若存在且 `FORCE_UPDATE=1`，则执行 PATCH（更新）；否则执行 SKIP（跳过）。

### 【脏数据物理隔离】
严禁在未经过滤的情况下将非相关岗位（如 DeepSeek 的行政、会计岗）写入 Notion。必须在列表页阶段进行 keyword 匹配：
```python
if any(k in title for k in ['AI', '产品经理', 'PM']):
```

# 🤖 Cline 核心操作规范 (InterviewOS 项目)

## 1. 终端执行红线 (绝对禁止静默执行)
* **严禁使用 `2>&1`**：在执行任何 Python 脚本或终端命令时，绝对不允许在命令末尾添加 `2>&1` 或任何重定向符号。必须让标准输出 (stdout) 和标准错误 (stderr) 实时、直接地打印在终端面板上。
* **拒绝黑盒等待**：绝不写没有任何终端回显的死循环代码。任何网络请求、爬虫循环、网关启动，都必须配有 `print` 或 `logger.info`。

## 2. 常驻进程 (Daemon/Gateway) 处理规范
* 遇到 `feishu_gateway.py` 这类 WebSocket 监听服务或死循环任务时，**禁止使用自动化 Agent 执行并等待**（这会导致卡死）。
* 你的职责是：编写代码 -> 检查语法 -> **提醒用户手动在终端中运行启动命令**。

## 3. 爬虫与网络请求原则
* **强制防卡死**：所有的网络请求 (requests, playwright 等) 必须设置 timeout (如 timeout=10)。
* **全面错误捕获**：关键启动节点和网络请求必须使用 try-catch 包裹，并打印完整的 exception trace，严禁吞噬报错信息。